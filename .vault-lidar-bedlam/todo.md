# Todo — lidar-bedlam

Todos live in [PROJECT.md](../PROJECT.md) (grouped by contribution, done items
at the bottom). Keep this file as a pointer only, so there is one list.

## Guardrails

- Keep dependencies minimal. Every new dep needs a rationale line in
  `HANDOFF.md`'s Confirmed Decisions table.
- New modules: append a node to `.vault-lidar-bedlam/lidar-bedlam.canvas`
  so the dashboard stays current.
- End of each work session: add one sentence to the log in `PROJECT.md`.
